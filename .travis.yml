language: node_js

node_js:
  - "stable"
  
cache:
  directories:
  - node_modules

script:
    - npm test
    - npm run build
    - npm run codecov --clear-cache
    
notifications:
  email: false
